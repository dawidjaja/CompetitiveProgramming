#include <algorithm>
#include <cstdio>

using namespace std;

long long n, m, t;

int main() {
	scanf("%d", &t);
	while(t--) {
		scanf("%lld%lld", &n, &m);
		double x = m*m;
		x /= ;
		printf("%lf\n", x);
	}
	return 0;
}