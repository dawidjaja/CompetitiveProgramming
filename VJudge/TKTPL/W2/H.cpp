#include <algorithm>
#include <cstdio>

using namespace std;

int main() {
	scanf("%d", &tc);
	while(tc--) {
		scanf("%d", &n);
		scanf("%d%d%d", &ss, &g, &time);
		scanf("%d", &m);
    for(int i = 1; i <= n; i++) {
      for(int j = 1; j <= time; j++) {
        
      }
    }
		for(int i = 1; i <= m; i++) {
			scanf("%d", &x);

			// masukin kota sink
		}
		scanf("%d", &r);
		for(int i = 1; i <= r; i++) {
			scanf("%d%d%d%d", &a, &b, &p, &ti);
			//hubungin a - b;
		}
	}
	return 0;
}
