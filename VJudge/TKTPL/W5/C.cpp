#include <algorithm>
#include <cstdio>

using namespace std;

int main() {
  scanf("%d", &n);
  getchar();
  for(int i = 0; i < n; i++) {
    list[i] = getchar_unlocked();
  }
  ptr = 0;
  for(int i = 1; i < n; i++) {

  }
  return 0;
}