#include <algorithm>
#include <cstdio>
#include <cstring>
#include <iostream>
using namespace std;
char awal[1000],del[1000],fain[1000],ripleis[1000];
int main ()
{
	scanf("%s%s%s%s",awal,del,fain,ripleis);
	for (int i=1;i<=awal;i++)
	{
		if (awal[i]==del[1])
		{
			cek=0;
			for (int j=1;j<=del.size();j++)
			{
				if (awal[i])
			}
		}
	}
	return 0;
}
