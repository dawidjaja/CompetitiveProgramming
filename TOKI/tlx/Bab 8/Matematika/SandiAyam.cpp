#include <algorithm>
#include <cstdio>
#include <stack>
using namespace std;
stack <int> s;
int main ()
{
	while (scanf("%s",&angka)!=EOF)
	{
		gets(asdf);
		if (angka[0]=='-')
		{
			temp+=5;
		}
		else if (angka[0]=='.')
		{
			s.push(temp);
			tmep=strlen(angka);
		}
		else if (angka[0]=='0')
		{
			
		}
	}
	return 0;
}
