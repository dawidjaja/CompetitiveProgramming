#include <algorithm>
#include <cstdio>
#include <vector>

using namespace std;

int main() {
	scanf("%d", &n);
	for(int i = 1; i <= n; i++) {
		scanf("%d%d", &list[i].fs, &list[i].sc);
		if()
	}
}