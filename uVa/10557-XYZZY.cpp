#include <algorithm>
#include <cstdio>
using namespace std;
int main()
{
	while(true)
	{
		scanf("%d",&n);
		if(n==-1)
		{
			return 0;
		}
		scanf("%d%d",&mcc[i],&d);
		for(int i=1;i<=n;i++)
		{
			scanf("%d",&v[i][v[i].size()]);
		}
		list[1]=100;
		for(int i=2;i<=100;i++)
		{
			list[i]=
		}
	}
	return 0;
}
