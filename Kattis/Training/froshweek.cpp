#include <algorithm>
#include <cstdio>
#include <cmath>
#include <map>

#define fs first
#define sc second
#define pii pair<int,int>

using namespace std;

int n, ans, list[100005], bit[100005];
map <int,int> tmpmap;

int main() {
	scanf("%d", &n);
	for (int i = 1; i <= n; i++) {
		scanf("%d", &list[i]);
	}

	return 0;
}