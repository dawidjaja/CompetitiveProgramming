#include <bits/stdc++.h>

using namespace std;

int main() {
    scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &m[i]);
        for (int j = 0; j < m[i]; j++) {
            scanf("%d%d%d", &item[i]
        }
    }
    return 0;
}
