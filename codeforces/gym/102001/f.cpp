#include <bits/stdc++.h>

using namespace std;

int main() {
    scanf("%d%d", &n, &m);
    for (int i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < n; i++) {
        scanf("%d", &b[i]);
    }
    int x = 0, y = 0;
    int lptr = 0, rptr = 0;
    while (
    return 0;
}

