#include <bits/stdc++.h>

using namespace std;

int f(int x) {
    if (x == 1) return 1;
    int tmp = 1;
    for (int i = 2; i <= 9; i++) {
        if (
    }
}

int main() {
    while (scanf("%lld\n", &x) != EOF) {
        ans = f(x, 1);
    }
}
